# Screenshots

Capture these four from a real run (app at http://localhost:5173, with a repo ingested) and save them
here with these exact names so the main README embeds them automatically:

| File | What to capture |
|---|---|
| `landing.png` | The landing screen — hero, ingest box, "Ask your codebase." |
| `workspace.png` | The workspace — left project panel (language donut, graph, stats) + a chat answer with citations |
| `code-viewer.png` | A citation clicked open — the sliding code viewer with the highlighted lines |
| `command-palette.png` | The ⌘K command palette open with some results |

Tip: index `app` (or a small GitHub repo), ask one question so the chat + panel are populated, then
capture. Use your OS screenshot tool (full window, no dev overlays). PNG, ideally ~1600px wide.

# Dev-only tools. Kept out of requirements.txt so the production image stays lean.
# These power our quality gate (pre-commit + CI). Pinned, like everything else.

# Lint + format in one fast tool (replaces flake8 + black + isort).
ruff==0.15.16

# Static type checking. Backs our "type hints on every function" rule.
mypy==2.1.0

# Security static analysis (flags risky patterns, hardcoded secrets, etc).
bandit==1.9.4

# Scans pinned dependencies for known CVEs.
pip-audit==2.10.0

# Coverage reporting on top of pytest (pytest itself is in requirements.txt).
pytest-cov==7.1.0

# Runs the fast checks automatically before each local commit.
pre-commit==4.6.0
